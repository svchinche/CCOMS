server:
  port: 8888


  
logging:
  level:
     org:
      springframework:
        web: DEBUG
         

spring:
  profiles:
    include: default
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/svchinche/org-mgmt-conf-files.git'
          search-paths:
          - 'config-files'  
         

