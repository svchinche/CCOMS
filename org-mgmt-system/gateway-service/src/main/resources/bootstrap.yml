spring:
  application:
    name: zuulserver-microservice
  profiles:
    include:
    - ${app.profile}
  cloud:
    config:
      fail-fast: true
      uri: http://cfg.ccoms.com:8888
      retry:
        max-attempts: 20
        max-interval: 10000